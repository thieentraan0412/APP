# Sao chép file này thành .env rồi điền giá trị thật.
# Vite chỉ expose biến có tiền tố VITE_ ra cho frontend.

# URL Worker (backend API), ví dụ: https://your-worker.your-name.workers.dev
VITE_WORKER_URL=

# Khoá cho thao tác ghi/quản lý (phải khớp secret API_KEY trên Worker)
VITE_API_KEY=
